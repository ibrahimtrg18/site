---
title: Welcome
description: Placeholder draft.
---

{/*
  DRAFT placeholder — the filename starts with "_", so it is skipped by the blog
  listing, the sitemap, and routing (see src/utils/content.ts).

  It exists only so `public/blogs/` contains at least one `.mdx` file, which the
  Turbopack build needs in order to resolve the dynamic `import()` used by the
  blog pages while there are no published posts yet. Once you publish a real
  post through the studio you can delete this file — just keep at least one
  `.mdx` in this folder.
*/}

# Welcome

This is a placeholder draft.
